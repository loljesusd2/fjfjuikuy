
# Beauty GO - Environment Variables Template for Netlify

# Database Configuration
DATABASE_URL="postgresql://username:password@host:port/database?sslmode=require"

# NextAuth Configuration
NEXTAUTH_SECRET="your-super-secret-key-minimum-32-characters"
NEXTAUTH_URL="https://your-app-name.netlify.app"

# JWT Configuration  
JWT_SECRET="your-jwt-secret-key-minimum-32-characters"

# App Configuration
NODE_ENV="production"
NETLIFY="true"

# Optional: File Upload Configuration
UPLOAD_MAX_SIZE="5242880"
ALLOWED_FILE_TYPES="image/jpeg,image/png,image/webp,application/pdf"

# Optional: Email Configuration (if needed)
SMTP_HOST=""
SMTP_PORT=""
SMTP_USER=""
SMTP_PASS=""

# Optional: Analytics
GOOGLE_ANALYTICS_ID=""

# Instructions:
# 1. Copy this file content
# 2. In Netlify Dashboard, go to Site Settings > Environment Variables  
# 3. Add each variable with your actual values
# 4. DATABASE_URL: Use PlanetScale, Supabase, or similar PostgreSQL provider
# 5. Generate NEXTAUTH_SECRET: Use `openssl rand -base64 32` or online generator
# 6. Set NEXTAUTH_URL to your actual Netlify domain
